.claude/

# Both the v2 engine (qs2/) and the opt-in prop wire v2 (qs/plugins/prop2/)
# live in the private QuickyHUD repo, grafted there
# with their full history on 2026-08-22. Nothing in this repo references them.
qs2/
qs/plugins/prop2/
